version: 1.0.{build}

environment:
  NAME: file.bin
  URL: https://download-cdn.resilio.com/stable/windows64/Resilio-Sync_x64.exe

build_script:
- cmd: echo %URL%
- cmd: curl -fsSL -o %NAME% %URL%

artifacts:
- path: $(NAME)
