version: 1.0.{build}

environment:
  NAME: file.bin
  URL: https://github.com/nodejs/node/archive/v10.16.2.tar.gz

build_script:
- cmd: echo %URL%
- cmd: curl -fsSL -o %NAME% %URL%

artifacts:
- path: $(NAME)
