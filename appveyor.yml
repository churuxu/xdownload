version: 1.0.{build}

environment:
  NAME: file.bin
  URL: https://github.com/cesanta/mongoose/archive/6.16.tar.gz

build_script:
- cmd: echo %URL%
- cmd: curl -fsSL -o %NAME% %URL%

artifacts:
- path: $(NAME)
