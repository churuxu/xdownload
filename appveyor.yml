version: 1.0.{build}

environment:
  NAME: file.bin
  URL: https://github.com/microsoft/vscode-cpptools/releases/download/0.27.0/cpptools-win32.vsix

build_script:
- cmd: echo %URL%
- cmd: curl -fsSL -o %NAME% %URL%

artifacts:
- path: $(NAME)
