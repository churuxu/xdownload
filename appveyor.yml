version: 1.0.{build}

environment:
  NAME: file.bin
  URL: https://github.com/RT-Thread/rt-thread/archive/v3.1.3.zip

build_script:
- cmd: echo %URL%
- cmd: curl -fsSL -o %NAME% %URL%

artifacts:
- path: $(NAME)
